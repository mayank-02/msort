# whatever flags you want to pass to the C compiler & linker
AM_CFLAGS = # -Wall
AM_LDFLAGS = -lm

# this lists the binaries to produce, the (non-PHONY, binary) targets in
# the previous manual Makefile
bin_PROGRAMS = msort
msort_SOURCES = msort.c msort.h
